import React from 'react';

const PrivateRoute = ({ children }) => {
    const user = null
    if (!user) {
        //navigate
    }
    // else
    return (children);
};

export default PrivateRoute;